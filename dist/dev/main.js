import React from "react";
import ReactDOM from "react-dom/client";
import { Button } from "../button";
ReactDOM.createRoot(document.getElementById("root")).render(React.createElement(Button, null));
